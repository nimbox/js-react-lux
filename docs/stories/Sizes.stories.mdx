import { useState } from 'react';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { CheckBar } from '../components/CheckBar';
import { RadioBar } from '../components/RadioBar';
import { Button } from '../components/Buttons';
import { controlScale } from '../components/ComponentScale';
import classnames from 'classnames';


export const CheckboxBarSample = ({ scale }) => {
    const [value, onChange] = useState([1]);
    return (
        <CheckBar scale={scale} value={value} onChange={onChange}>
            <CheckBar.Option value={1}>1</CheckBar.Option>
            <CheckBar.Option value={2}>2</CheckBar.Option>
            <CheckBar.Option value={3}>3</CheckBar.Option>
            <CheckBar.Option value={6}>6</CheckBar.Option>
            <CheckBar.Option value={12}>12 months</CheckBar.Option>
        </CheckBar>
    );
};

export const RadioBarSample = ({ scale }) => {
    const [value, onChange] = useState([1]);
    return (
        <RadioBar scale={scale} value={value} onChange={onChange}>
            <RadioBar.Option value={1}>1</RadioBar.Option>
            <RadioBar.Option value={2}>2</RadioBar.Option>
            <RadioBar.Option value={3}>3</RadioBar.Option>
            <RadioBar.Option value={6}>6</RadioBar.Option>
            <RadioBar.Option value={12}>12 months</RadioBar.Option>
        </RadioBar>
    );
};


<Meta title="Standards/Scales and Paddings"/>

# Scales

We are using a basic font scale for `html` of `14px`.  Most sites use `16px` as
the default font scale, but since our applications are very data intensive we
need to pack more density. We tried `12px` but the smaller scales were really to
small.

When applying the default scalings from tailwind we end up with these scales. We
are only using `rems` as units so `px` are only for reference.

<Canvas>
    <div>
        <div className="text-xs">The quick brown fox jumps over the lazy dog (xs 0.75rem 10.5px)</div>
        <div className="text-sm">The quick brown fox jumps over the lazy dog (sm 0.875rem 12.5px)</div>
        <div className="text-base">The quick brown fox jumps over the lazy dog (base 1rem 14px)</div>
        <div className="text-lg">The quick brown fox jumps over the lazy dog (lg 1.125rem 15.75px)</div>
        <div className="text-xl">The quick brown fox jumps over the lazy dog (xl 1.75rem 17.5px)</div>
    </div>
</Canvas>

# Paddings

Each scale requires different paddings to make sure that the space around the
text looks ok. Buttons, Inputs, and other elements should use these defaults for
their paddings. In fact to standardize this we will use the `paddings[]` object
that given a scale (`xs`, `sm`, `base`, `lg`) provides the font scale and  padding
classes.

We are using these defaults:

### Extra Small

<Canvas>
<Story name="Extra Small">
<div className="mb-4">{controlScale['xs']}</div>
<div className="flex flex-row items-center space-x-2">
    <div className="text-xs border px-2 py-0.5 rounded border-gray-500">Text</div>
    <Button scale="xs">Button</Button>
    <Button scale="xs" secondary>Button</Button>
    <CheckboxBarSample scale="xs"/>
    <RadioBarSample scale="xs"/>
</div>
</Story>
</Canvas>

### Small

<Canvas>
<Story name="Small">
<div className="mb-4">{controlScale['sm']}</div>
<div className="flex flex-row items-center space-x-2.5">
    <div className="text-sm border px-2.5 py-1 rounded border-gray-500">Text</div>
    <Button scale="sm">Button</Button>
    <Button scale="sm" secondary>Button</Button>
    <CheckboxBarSample scale="sm"/>
    <RadioBarSample scale="sm"/>
</div>
</Story>
</Canvas>

### Base

<Canvas>
<Story name="Base">
<div className="mb-4">{controlScale['base']}</div>
<div className="flex flex-row items-center space-x-3">
    <div className="inline-block border px-3 py-2 rounded border-gray-500">Text</div>
    <Button>Button</Button>
    <Button secondary>Button</Button>
    <CheckboxBarSample/>
    <RadioBarSample/>
</div>
</Story>
</Canvas>

### Large

<Canvas>
<Story name="Large">
<div className="mb-4">{controlScale['lg']}</div>
<div className="flex flex-row items-center space-x-4">
    <div className="text-lg border px-4 py-2 rounded border-gray-500">Text</div>
    <Button scale="lg">Button</Button>
    <Button scale="lg" secondary>Button</Button>
    <CheckboxBarSample scale="lg"/>
    <RadioBarSample scale="lg"/>
</div>
</Story>
</Canvas>
