import classnames from 'classnames';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { CheckButton } from './CheckButton';


export const Label = (props) => <span className={classnames('border-t border-b', props.className)}>{props.children}</span>;


<Meta title="Component/Controls/CheckButton" 
    component={CheckButton} 
    argTypes={{ 
        onFulfill: { 
            action: 'fulfilled' 
        },
        onReject: { 
            action: 'rejected' 
        }
    }}
/>

# CheckButton

A better control for ending tasks.

export const Template = (args) => <span><CheckButton {...args} data-tooltip-fulfill="Fulfill" data-tooltip-reject="Reject"/> Do this task</span>;

<Canvas>
    <Story name="Simple">
        {Template.bind({})}
    </Story>
</Canvas>
    