import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Button } from './Buttons';
import { EditIcon, AngleRightIcon } from '../icons';


<Meta title="Component/Buttons" 
    component={Button} 
/>

# Buttons

Buttons are everywhere

export const Template = ({ children, ...args }) => <Button {...args}>{children}</Button>;

<Canvas>
    <Story name="Template" args={{ color: 'primary', variant: 'filled', children: 'Content' }} parameters={{ layout:'centered' }}>
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Sizes">
        <div className="flex flex-row justify-between items-baseline">
            <span className="text-xs">Extra Small <Button>Extra Small</Button></span>
            <span className="text-sm">Small <Button>Small</Button></span>
            <span className="text-base">Base <Button>Base</Button></span>
            <span className="text-lg">Large <Button>Large</Button></span>
            <span className="text-xl">Extra Large <Button>Extra Large</Button></span>
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="Variants">
        <div className="space-y-2">
            <div className="flex flex-row justify-between items-baseline">
                <Button color="primary" variant="filled">filled</Button>
                <Button color="primary" variant="text">text</Button>
                <Button color="primary" variant="outlined">Outlined</Button>
                <Button color="primary" variant="link">Link</Button>
            </div>
            <div className="flex flex-row justify-between items-baseline">
                <Button color="secondary" variant="filled">filled</Button>
                <Button color="secondary" variant="text">text</Button>
                <Button color="secondary" variant="outlined">Outlined</Button>
                <Button color="secondary" variant="link">Link</Button>
            </div>
            <div className="flex flex-row justify-between items-baseline">
                <Button color="muted" variant="filled">filled</Button>
                <Button color="muted" variant="text">text</Button>
                <Button color="muted" variant="outlined">Outlined</Button>
                <Button color="muted" variant="link">Link</Button>
            </div>
        </div>
    </Story>
</Canvas>

Icons, or any other react node, can be placed at the start or at the end of the 
button. The placement is done with a flex row layou in which the only growing 
part is the children. The child elements are wrapped inside a span to make sure
that it can grow. 

<Canvas>
    <Story name="With icons">
        <div className="text-center space-x-2 text-base">
            <Button color="muted" variant="outlined" start={<EditIcon className="stroke-2"/>}>Edit</Button>
            <Button color="muted" variant="outlined" end={<AngleRightIcon className="stroke-2"/>}>Continue</Button>
        </div>
    </Story>
</Canvas>


<Canvas>
    <Story name="With shared baseline">
        <div className="space-y-2">
            <div className="text-center space-x-2 text-base">
                <span>Edit</span>
                <Button color="muted" variant="outlined">Edit</Button>
                <Button color="muted" variant="outlined" start={<EditIcon className="stroke-2"/>}>Edit</Button>
                <Button color="muted" variant="outlined" end={<AngleRightIcon className="stroke-2"/>}>Continue</Button>
                <Button color="muted" variant="outlined">Continue</Button>
                <span>Continue</span>
            </div>
            <div className="text-center space-x-2 text-base">
                <span>Edit</span>
                <Button color="muted" variant="text">Edit</Button>
                <Button color="muted" variant="text" start={<EditIcon className="stroke-2"/>}>Edit</Button>
                <Button color="muted" variant="text" end={<AngleRightIcon className="stroke-2"/>}>Continue</Button>
                <Button color="muted" variant="text">Continue</Button>
                <span>Continue</span>
            </div>
        </div>
    </Story>
</Canvas>



<Canvas>
    <Story name="All are same height">
        <div className="space-y-2">
            <div className="text-center space-x-2 text-base">
                <Button variant="filled">Filled</Button>
                <Button variant="outlined">Outlined</Button>
                <Button variant="text">Text</Button>
                <Button variant="link">Link</Button>
            </div>
        </div>
    </Story>
</Canvas>
