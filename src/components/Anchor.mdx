import { Meta, Controls, Stories, Primary } from '@storybook/addon-docs/blocks';
import * as AnchorStories from './Anchor.stories';

<Meta name="Documentation" of={AnchorStories} />

# Anchors

Anchors are like buttons but the behave as links. In order to use them with
react router, the internal component can be changed. It is usually `a` but can
be changed to `Link` from `react-router-dom`.

<Primary />

## Inputs

The component accepts the following inputs (props):

<Controls />

## Additional variations

Listed below are additional variations of the component.

<Stories />
