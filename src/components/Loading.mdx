import { Meta, Controls, Stories, Primary } from '@storybook/addon-docs/blocks';
import * as LoadingStories from './Loading.stories';


<Meta name="Documentation" of={LoadingStories} />

# Loading

Use to signal that is something is happening in the background. This can be used
to signal that a page is loading, or that an action is being processed.

<Primary />

## Inputs

The component accepts the following inputs (props):

<Controls />

## Additional variations

Listed below are additional variations of the component.

<Stories />
